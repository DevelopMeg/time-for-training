import React from "react";

const ErrorData = () => {
  return (
    <div className="section-error">
      <p className="section-error__text">Unfortunately data didn't download</p>
    </div>
  );
};

export default ErrorData;
